package mars

// finds the lowest of two integers
func min(a, b int) int {
	if a < b {
		return a
	} else {
		return b
	}
}

// finds the largest of two integers
func max(a, b int) int {
	if a > b {
		return a
	} else {
		return b
	}
}
